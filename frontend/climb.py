# Dependencies
import streamlit as st

if __name__ == '__main__':
    # Run the application itself
    st.set_page_config(
        page_title='Main page'
        )

    st.markdown(
    """
    # Gym climbing logging application

    Tell something about the page here and what you can find on the other pages.
    """
    )